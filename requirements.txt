# Core web stack
fastapi>=0.112.0
uvicorn>=0.30.0
pydantic>=2.8.2

# HTTP client
requests>=2.32.3

# Numerics
numpy>=1.24.0  # vectors / arrays

# Chemistry (optional tooling)
rdkit>=2024.03.1  # RDKit cheminformatics

# Config
PyYAML>=6.0  # config/settings.yaml
python-dotenv>=1.0.0  # load .env

# Embeddings / HF ecosystem
sentence-transformers>=3.0.1  # backend/embedder.py
transformers>=4.44.0  # pulled in by sentence-transformers
accelerate>=0.33.0  # pulled in by sentence-transformers
huggingface_hub>=0.24.5  # pulled in by sentence-transformers
modelscope>=1.9.0  # optional ModelScope snapshots (embedder.py)

# Vector search (CPU build; use faiss-gpu in CUDA-only setups if you switch)
faiss-cpu>=1.7.4  # persona_store semantic index

# Local LLM server (optional)
# The app talks to vLLM over HTTP; this package is only needed if you run vLLm locally.
# Safe to drop when using remote APIs only (e.g. DeepSeek).
vllm>=0.5.0

# Browser automation (playwright tools)
playwright>=1.40.0

# IMAP (ID command for some providers, e.g. NetEase 163)
imapclient>=2.3.0

# Tests
pytest>=8.3.0
