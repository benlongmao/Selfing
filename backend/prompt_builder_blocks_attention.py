from backend.prompt_builder_core import logger


def build_attention_block(self_model, session_id: str) -> str:
    """
    [Disabled 2026-02-02] The attention dimension was removed from z_self.

    Signature kept for callers; always returns an empty string.
    """
    return ""

